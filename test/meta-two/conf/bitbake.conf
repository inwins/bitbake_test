require local.conf
include conf/might_exist.conf
#TMPDIR="${TOPDIR}/tmp"
#CACHE ="${TMPDIR}/cache"
#
## 指定用于创建配方戳文件的基本路径。实际戳文件的路径是通过对该字符串求值，然后附加附加信息来构建的。
#STAMP   = "${TMPDIR}/${PN}/stamps"
## 指向BitBake在构建特定é方时放置临时文件的目录，这些文件主要由任务日志和脚本组成。
#T = "${TMPDIR}/${PN}/work"
#
